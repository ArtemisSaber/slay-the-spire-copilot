#!/usr/bin/env sh
set -eu

echo "pre-commit: cargo fmt --check"
cargo fmt --check

echo "pre-commit: cargo clippy -- -D warnings"
cargo clippy -- -D warnings

echo "pre-commit: cargo test"
cargo test

echo "pre-commit: cargo build --release"
cargo build --release

echo "pre-commit: local CI pipeline passed"
